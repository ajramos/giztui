# golangci-lint configuration
version: "2"

run:
  timeout: 10m
  go: "1.25"

linters:
  default: none
  enable:
    - govet
    - errcheck
    - gosec
    - staticcheck
    - unused
    - ineffassign
    - misspell
